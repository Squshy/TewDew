mutation UpdateTask($taskId: UUID!, $title: String, $completed: Boolean) {
  updateTask(taskId: $taskId, title: $title, completed: $completed) {
    __typename
    ... on Task {
      ...SingleTask
    }
    ... on UpdateTaskError {
      title
    }
  }
}
