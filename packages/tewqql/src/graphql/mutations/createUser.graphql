mutation CreateUser($username: String!, $password: String!) {
  createUser(username: $username, password: $password) {
    __typename
    ... on AuthUser {
      ...SingleAuthUser
    }
    ... on NewUserError {
      username
      password
    }
  }
}
